name: Build & Push to Docker Hub

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read # required para sa checkout

    steps:
      - name: Checkout files
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: raphmragos/virgozkipanel:latest
          cache-from: type=registry,ref=raphmragos/virgozkipanel:buildcache
          cache-to: type=registry,ref=raphmragos/virgozkipanel:buildcache,mode=max
